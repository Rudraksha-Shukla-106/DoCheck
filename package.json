{
  "name": "doccheck",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy",
    "deploy:pages": "astro build && wrangler pages deploy dist --project-name=doccheck"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^5.13.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "wrangler": "^4.130.0"
  }
}
